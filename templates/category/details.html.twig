{% extends 'base.html.twig' %}

{% block title %}Détails pour la catégorie {{category.id}}{% endblock %}

{% block body %}
<h1>{{category.title}}</h1>


{% for quiz in category.quizzes %}
 <a href="{{ path("quiz_details", {id: quiz.id})}}">{{quiz.title}}</a>
{% endfor %}

{% endblock %}
